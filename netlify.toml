[build]
  # Internal build disabled to save credits; handled by GitHub Actions
  command = ""
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "stable"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
